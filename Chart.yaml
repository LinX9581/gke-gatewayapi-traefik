apiVersion: v2
name: traefik-gatewayapi
description: Minimal Traefik + Gateway API chart for ArgoCD deployments
type: application
version: 0.1.0
appVersion: "3.1"
dependencies:
  - name: traefik
    version: "34.3.0"
    repository: "https://traefik.github.io/charts"
